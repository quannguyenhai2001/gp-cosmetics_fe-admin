import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(theme => ({
    helperText: {
        position: "absolute",
        top: 40,
        margin: 0,
        minWidth: "350px",
    },
}));